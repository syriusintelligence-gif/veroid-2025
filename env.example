# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Note: These variables MUST be prefixed with VITE_ to be accessible in the frontend
# They are embedded during build time, so any changes require a rebuild/redeploy